version: '3.8'

services:
  pdf-service:
    build: .
    container_name: pdf_service_backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
